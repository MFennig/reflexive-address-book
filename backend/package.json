{
  "name": "reflexive-address-book-backend",
  "version": "1.0.0",
  "description": "Template repository for bootstrapping a new API. Supports `Lambda` and `Kubernetes` deployments.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "path": "^0.12.7",
    "sqlite": "^4.0.8",
    "sqlite3": "^4.2.0"
  }
}
